# Prompt

Acting as **[WPF/WinAPI Expert]**: implement MVP-4 per `docs/ROADMAP.md` — WPF Desktop utility with MVVM, EBS API client (ApiKey), WoW foreground injection (`PostMessage` primary, `SendInput` fallback), `<255` char `/run` chunking, single `WoWChatLog.txt` tail for `[MGM_WHO]` / `[MGM_ACCEPT]` / `[MGM_CONFIRM]`, explicit Sync/Inject → `InProgress`, manual overrides, and documentation/readiness updates.
