# Prompt

Second interactive pass to close remaining doc/spec risks. User answered:

- **A.1 A** — uniform random spin candidate among active pool rows.
- **B.2 A** — **`verify-candidate`** 30s grace anchored to the **UTC spin boundary** that **closes** the cycle’s verification window.
- **C.3** — user unsure what “minified JSON” meant; clarified in **`docs/SPEC.md`** §8 in plain language (one continuous log line, structured `{ … }` fields).
- **D** — user unsure about “whisper trigger”; locked: **Desktop** injects **`/run NotifyWinnerWhisper("<payoutId>","<characterName>")`**; **addon** executes the §9 **`/whisper`** (addon types Russian text, not Desktop).
- **E.4 A** — refresh **`INTERACTION_SCENARIOS.md`** (SC-014 mock-auth note removed; SC-016/TC notes aligned to SPEC **`503`**).
- **E.5 YES** — SPEC §5 error model documents **`503`** + Extension behavior.
- **F.6 YES** — **MediatR** handlers **Backend only**; Shared = contracts/validation.
- **G.7 YES** — MVP-5 **viewer panel only**; **no** UI-201–204.
- **G.8 YES** — treated as duplicate confirm of G.7.

Agent: apply edits to canonical docs and add this history entry.
