# Prompt

User request (original thread): Implement **MVP-2** from `docs/ROADMAP.md` (`MimironsGoldOMatic.Backend`).

Continuation: finish compile fixes, JWT/dev key behavior, FluentValidation registration, Marten `StreamIdentity` namespace, EventSub `[AllowAnonymous]`, expiration job first run, global rate limiter (EventSub exempt), sample `appsettings`, and doc parity (`ROADMAP`, `IMPLEMENTATION_READINESS`, Backend `ReadME`).
