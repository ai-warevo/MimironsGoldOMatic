# Plan

1. Insert **#### File Structure** under **C#/.NET Coding Standards (Backend & WPF)** after **Formatting and layout**.
2. Preserve user bullets; add a **This repository** clause for mandatory `MimironsGoldOMatic.*` roots and pointer to `PROJECT_STRUCTURE.md`.
3. Use a file-scoped namespace example aligned with this repo (not `MyProject`).
