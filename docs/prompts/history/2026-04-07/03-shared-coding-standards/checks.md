# Checks

- [x] Review all `.cs` in `MimironsGoldOMatic.Shared`
- [x] `dotnet format` on Shared project
- [x] Apply consistent layout (enum trailing comma; multiline records)
- [x] `dotnet build src/MimironsGoldOMatic.slnx` succeeds
