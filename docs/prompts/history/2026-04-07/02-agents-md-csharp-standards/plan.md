# Plan

1. Insert a shared **C#/.NET Coding Standards (Backend & WPF)** subsection under `AGENTS.md` §8, before **[Backend/API Expert]**, so both roles reference one canonical block.
2. Add an intro line that defers to existing project rules (`MimironsGoldOMatic.*` namespaces) where they conflict with generic guidelines.
3. Light editorial pass: consistent headings, spell “standards,” minor clarity on LINQ and `Task.CompletedTask` wording.
4. Update the HTML “Updated” comment at top of `AGENTS.md`.

**Risk:** Low (documentation only).
