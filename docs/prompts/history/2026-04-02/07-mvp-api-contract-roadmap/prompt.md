## Request

1) Add an "MVP API Contract" section to the documentation (endpoints, auth expectations, idempotency, statuses, and common errors).

2) Save the finalized project roadmap into `docs/` (outside `docs/prompts/`).

