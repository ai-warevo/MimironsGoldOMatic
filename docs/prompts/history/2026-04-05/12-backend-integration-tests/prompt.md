# Prompt

Continue Backend integration test work: wire solution/CI, fix failing tests (rate limiting, EventSub secret, DB isolation), add performance smoke and `Backend.IntegrationTests/README.md`, align docs.

(User task: dedicated integration project, Testcontainers PostgreSQL, HTTP/scenario coverage, cleanup, benchmarks, README.)
