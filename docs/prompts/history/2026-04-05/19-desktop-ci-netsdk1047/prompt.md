# User request

Fix Windows CI failure in `test-desktop` job: NETSDK1047 — Desktop `project.assets.json` missing target `net10.0-windows/win-x64` when building `MimironsGoldOMatic.Desktop.UnitTests` in Release after two-step restore.
