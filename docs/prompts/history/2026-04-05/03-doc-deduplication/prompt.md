# Task

Deduplicate project `.md` documentation (exclude `docs/prompts/`): centralize repeated blocks into `docs/*.md` and replace originals with `<!-- Content moved to … -->` references per user request.
