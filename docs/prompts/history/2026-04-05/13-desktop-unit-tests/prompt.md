# Prompt

**Task:** Generate Unit tests for Desktop application business logic.

**Goal:** Cover core Desktop functionality with fast, isolated tests.

**Output:** New project `src/tests/MimironsGoldOMatic.Desktop.UnitTests/`, tests for ViewModels/Services/Helpers, mocks for API and filesystem (temp dirs), edge cases (empty/invalid/timeout), README with run instructions.
