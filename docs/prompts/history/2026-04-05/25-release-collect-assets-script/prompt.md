# Prompt

Refactor the `create-release` job step in `.github/workflows/release.yml` (collect ZIPs, Docker tar, checksums, release notes): add a script under `.github/scripts` and invoke it from the workflow.
