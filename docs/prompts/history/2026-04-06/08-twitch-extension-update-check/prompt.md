You are an expert **Twitch Extension** developer working with **React + Vite + TypeScript** in the `MimironsGoldOMatic.TwitchExtension` project.

Task: implement a **“Check for Updates”** feature for the viewer-facing Twitch Extension that uses the EBS version endpoint (`GET /api/version`).

Source request: `tmp/cursor_prompt_twitch_extension_updates.md` (lines 1-225).

