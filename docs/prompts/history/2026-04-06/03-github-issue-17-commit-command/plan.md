# Plan

1. Rewrite `.cursor/commands/commit.md` with CC v1.0.0 workflow, types table, breaking-change rules, scope heuristics, footer order, body/length rules, examples, Unix + PowerShell output.
2. Align `.cursor/rules/git-commit-footer.mdc` so standard footers precede custom metadata (avoid contradicting the command).

Risks: none (docs/rules only).
