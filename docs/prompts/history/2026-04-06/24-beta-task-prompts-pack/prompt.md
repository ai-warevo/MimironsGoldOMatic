User request:

"now create tasks inside `docs/beta` directory
1 file = 1 task = 1 prompt for cursor"

Context:
- User wants a set of runnable prompt files for Cursor.
- Each task must be isolated in its own file.
