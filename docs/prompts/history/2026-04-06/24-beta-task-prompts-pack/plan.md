Plan:

1. Create a beta task pack in `docs/beta/` with one markdown file per task prompt.
2. Align tasks to current beta scope and go/no-go criteria.
3. Ensure each file is directly executable as a Cursor prompt (goal, inputs, constraints, acceptance criteria, output artifacts).
4. Add an index file listing recommended execution order.
