User request:

"Automate assigning a version to each component" by updating release automation, specifically around:
- `.github/workflows/release.yml`
- `.cursor/skills/github-release/scripts/release-skill.js`

Implementation requested: proceed now.

