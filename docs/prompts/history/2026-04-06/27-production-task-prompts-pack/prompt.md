User request:

"read @docs/overview/ROADMAP.md and generate tasks for Production too like you did it for Beta (`docs/beta`)

output directory will be `docs/prod`"

Context:
- Mirror the Beta one-file-per-task prompt style.
- Source of truth for Production scope is `docs/overview/ROADMAP.md` Production milestone section.
