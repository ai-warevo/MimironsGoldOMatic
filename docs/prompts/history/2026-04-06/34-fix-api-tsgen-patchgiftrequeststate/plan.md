## Plan

- Update `MimironsGoldOMatic.ApiTsGen` so `Patch*` DTOs are eligible for emission into `models.ts`.
- Regenerate TS artifacts via `dotnet build src/MimironsGoldOMatic.slnx`.
- Ensure the Twitch Extension builds (`npm run build`) without manual compatibility hacks.
- Update `checks.md` as work progresses and finish with `report.md` including verification + DoD.

