# Action: Phase 3 - Implementation of Backend API
Acting as [Backend/API Expert], please read @docs/MimironsGoldOMatic.Backend/ReadME.md and reference @MimironsGoldOMatic.Shared.

Create the ASP.NET Core (.NET 10) Web API project `MimironsGoldOMatic.Backend` in `/src`.
- Configure EF Core with PostgreSQL based on the schema in the doc.
- Implement `PayoutController` with /claim, /pending, and /status endpoints.
- Ensure it uses the Shared DTOs for request/response bodies.
