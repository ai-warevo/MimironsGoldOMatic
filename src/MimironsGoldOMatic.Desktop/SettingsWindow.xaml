<Window x:Class="MimironsGoldOMatic.Desktop.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings — Mimiron's Gold-o-Matic"
        Height="420" Width="520"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock Text="API base URL" FontWeight="SemiBold"/>
    <TextBox x:Name="BaseUrlBox" Grid.Row="1" Margin="0,4,0,12"/>
    <TextBlock Grid.Row="2" Text="X-MGM-ApiKey (stored with DPAPI)" FontWeight="SemiBold"/>
    <PasswordBox x:Name="ApiKeyBox" Grid.Row="3" Margin="0,4,0,12"/>
    <TextBlock Grid.Row="4" Text="WoW install folder (contains Logs\WoWChatLog.txt)" FontWeight="SemiBold"/>
    <TextBox x:Name="WowDirBox" Grid.Row="5" Margin="0,4,0,12"/>
    <TextBlock Grid.Row="6" Text="WoW chat log override (file or directory)" FontWeight="SemiBold"/>
    <TextBox x:Name="LogOverrideBox" Grid.Row="7" Margin="0,4,0,12"/>
    <StackPanel Grid.Row="8" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,8,0,0">
      <TextBlock Text="Poll interval (sec):" VerticalAlignment="Center" Margin="0,0,8,0"/>
      <TextBox x:Name="PollBox" Width="56" VerticalAlignment="Center"/>
      <TextBlock Text="Injection:" VerticalAlignment="Center" Margin="16,0,8,0"/>
      <ComboBox x:Name="InjectionCombo" Width="140" VerticalAlignment="Center"/>
    </StackPanel>
    <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Test connection" Width="120" Margin="0,0,8,0" Click="Test_Click"/>
      <Button Content="OK" Width="80" IsDefault="True" Click="Ok_Click"/>
      <Button Content="Cancel" Width="80" Margin="8,0,0,0" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
